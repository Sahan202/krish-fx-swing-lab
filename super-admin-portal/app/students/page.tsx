import Link from 'next/link';
import { createClient } from '@supabase/supabase-js';
import StudentActions from './student-actions';
export const dynamic = 'force-dynamic';
export default async function StudentsPage() { const key = process.env.SUPABASE_SERVICE_ROLE_KEY; const admin = key ? createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!, key, { auth: { autoRefreshToken: false, persistSession: false } }) : null; const { data: students } = admin ? await admin.from('profiles').select('id,full_name,phone,badge,approval_status,created_at').eq('role', 'student').order('created_at', { ascending: false }) : { data: [] }; return <main className="min-h-screen px-6 py-12"><div className="mx-auto max-w-6xl"><Link href="/dashboard" className="text-sm text-blue-400">← Super Admin Dashboard</Link><h1 className="mt-8 text-4xl font-bold">Students</h1><p className="mt-2 text-slate-400">Manage student records and passwords inside this portal.</p><div className="mt-8 space-y-3">{students?.map((student) => <div key={student.id} className="flex flex-col gap-4 rounded-2xl border border-slate-800 bg-slate-900 p-5 lg:flex-row lg:items-center lg:justify-between"><div><p className="font-semibold">{student.full_name ?? 'Student'}</p><p className="text-sm text-slate-400">{student.phone ?? 'No WhatsApp'} · Badge {student.badge ?? '-'} · {student.approval_status}</p></div><StudentActions id={student.id} /></div>)}</div></div></main>; }
