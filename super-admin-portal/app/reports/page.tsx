import Link from 'next/link';
import { serviceClient } from '@/lib/admin-audit';
import PortalWorkspace from '../portal-workspace';
export const dynamic = 'force-dynamic';
export default async function ReportsPage(){const {data:logs}=await serviceClient().from('admin_audit_logs').select('*').order('created_at',{ascending:false}).limit(500);const time=(v:string)=>new Intl.DateTimeFormat('en-GB',{timeZone:'Asia/Colombo',dateStyle:'medium',timeStyle:'medium'}).format(new Date(v));return <PortalWorkspace><div className="mx-auto max-w-7xl"><Link href="/dashboard" className="text-sm font-semibold text-cyan-300">← Control center</Link><div className="mt-8 flex items-end justify-between"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-cyan-300">Security intelligence</p><h1 className="mt-2 text-4xl font-bold">Audit reports</h1><p className="mt-2 text-slate-400">Every recorded admin and student activity in Sri Lanka time.</p></div><Link href="/reports" className="rounded-xl bg-cyan-400 px-4 py-3 text-sm font-bold text-slate-950">Refresh</Link></div><div className="mt-8 overflow-x-auto rounded-3xl border border-slate-800 bg-[#081322]"><table className="w-full min-w-[900px] text-left text-sm"><thead className="border-b border-slate-800 text-xs uppercase tracking-wider text-slate-500"><tr><th className="p-5">Date & time</th><th className="p-5">User</th><th className="p-5">Role</th><th className="p-5">Action</th><th className="p-5">Target</th><th className="p-5">Details</th></tr></thead><tbody>{logs?.map(log=><tr key={log.id} className="border-b border-slate-800/80"><td className="p-5 text-slate-400">{time(log.created_at)}</td><td className="p-5 font-medium text-white">{log.actor_email??'System'}</td><td className="p-5"><span className="rounded-full bg-cyan-400/10 px-3 py-1 text-xs font-bold text-cyan-300">{log.actor_role??'visitor'}</span></td><td className="p-5 text-cyan-300">{log.action}</td><td className="p-5 text-slate-300">{log.target_type}</td><td className="max-w-xs break-words p-5 text-xs text-slate-500">{JSON.stringify(log.details)}</td></tr>)}{!logs?.length&&<tr><td colSpan={6} className="p-10 text-center text-slate-500">No activity yet.</td></tr>}</tbody></table></div></div></PortalWorkspace>}
