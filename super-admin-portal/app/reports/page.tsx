import Link from 'next/link';
import { serviceClient } from '@/lib/admin-audit';

export const dynamic = 'force-dynamic';
export default async function ReportsPage() {
  const { data: logs } = await serviceClient().from('admin_audit_logs').select('*').order('created_at', { ascending: false }).limit(500);
  return <main className="min-h-screen px-6 py-12"><div className="mx-auto max-w-7xl"><Link href="/dashboard" className="text-sm text-blue-300">← Super Admin Dashboard</Link><div className="mt-8 flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-bold uppercase tracking-[.2em] text-blue-400">Security & activity</p><h1 className="mt-2 text-4xl font-bold">Audit report</h1><p className="mt-2 text-slate-400">Login history and every recorded portal action. Latest 500 events.</p></div><Link href="/reports" className="rounded-xl bg-blue-600 px-4 py-3 text-sm font-bold text-white">Refresh report</Link></div><div className="mt-8 overflow-x-auto rounded-3xl border border-slate-800 bg-slate-900"><table className="w-full min-w-[850px] text-left text-sm"><thead className="border-b border-slate-800 text-xs uppercase tracking-wider text-slate-400"><tr><th className="p-4">Date & time</th><th className="p-4">Admin</th><th className="p-4">Admin ID</th><th className="p-4">Action</th><th className="p-4">Target</th><th className="p-4">Details</th></tr></thead><tbody>{logs?.map((log) => <tr key={log.id} className="border-b border-slate-800/70 align-top"><td className="p-4 whitespace-nowrap text-slate-300">{new Date(log.created_at).toLocaleString()}</td><td className="p-4">{log.actor_email ?? 'Unknown'}</td><td className="p-4 font-mono text-xs text-slate-400">{log.actor_id ?? '-'}</td><td className="p-4"><span className="rounded-full bg-blue-500/15 px-3 py-1 font-bold text-blue-300">{log.action}</span></td><td className="p-4">{log.target_type}<br/><span className="font-mono text-xs text-slate-500">{log.target_id ?? '-'}</span></td><td className="p-4 max-w-xs break-words text-slate-400">{JSON.stringify(log.details)}</td></tr>)}{!logs?.length && <tr><td colSpan={6} className="p-8 text-center text-slate-400">No activity yet. Log in or make a portal change to start the report.</td></tr>}</tbody></table></div></div></main>;
}
