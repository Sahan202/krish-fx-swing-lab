import Link from 'next/link';
import { createClient } from '@supabase/supabase-js';
import ApplicationActions from './application-actions';
export const dynamic = 'force-dynamic';
export default async function PortalDashboard() { const key = process.env.SUPABASE_SERVICE_ROLE_KEY; const admin = key ? createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!, key, { auth: { autoRefreshToken: false, persistSession: false } }) : null; const { data: applications } = admin ? await admin.from('student_applications').select('id,full_name,email,whatsapp_number,badge,status').order('created_at', { ascending: false }) : { data: [] }; return <main className="min-h-screen px-6 py-12"><div className="mx-auto max-w-6xl"><p className="text-sm font-bold uppercase tracking-[.2em] text-blue-400">Krish FX Swing Lab</p><h1 className="mt-3 text-4xl font-bold">Super Admin Control Center</h1><p className="mt-2 text-slate-400">All administration stays inside this portal.</p><nav className="mt-10 grid gap-4 sm:grid-cols-3"><Link href="/students" className="rounded-2xl bg-blue-600 p-6 text-lg font-bold text-white">Students</Link><Link href="/content" className="rounded-2xl bg-slate-900 p-6 text-lg font-bold text-white">Courses & Lessons</Link><Link href="/dashboard" className="rounded-2xl border border-slate-700 p-6 text-lg font-bold text-blue-300">Applications</Link></nav><section className="mt-10 rounded-3xl border border-slate-800 bg-slate-900 p-7"><h2 className="text-2xl font-bold">Student applications</h2><div className="mt-6 space-y-3">{applications?.map((application) => <div key={application.id} className="flex flex-col gap-4 rounded-2xl bg-slate-950 p-4 lg:flex-row lg:items-center lg:justify-between"><div><p className="font-semibold">{application.full_name}</p><p className="text-sm text-slate-400">{application.email} · {application.whatsapp_number} · Badge {application.badge}</p></div><div className="flex items-center gap-3"><span className="rounded-full bg-blue-500/15 px-3 py-1 text-xs text-blue-300">{application.status}</span><ApplicationActions id={application.id} status={application.status} /></div></div>)}{!applications?.length && <p className="text-sm text-slate-400">No applications yet.</p>}</div></section></div></main>; }
