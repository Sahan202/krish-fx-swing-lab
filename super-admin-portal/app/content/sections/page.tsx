import Link from 'next/link';import {createClient} from '@supabase/supabase-js';import SectionsManager from '../sections-manager';import {requirePortalPageAccess} from '@/lib/portal-page-access';
export default async function SectionsPage(){await requirePortalPageAccess({permission:'manage_content'});const admin=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.SUPABASE_SERVICE_ROLE_KEY!);const[{data:courses},{data:sections}]=await Promise.all([admin.from('courses').select('id,title').order('title'),admin.from('course_sections').select('id,course_id,title,description,section_order').order('section_order')]);return <main className="min-h-screen bg-[#030914] p-8 text-white"><Link href="/content" className="text-cyan-300">← Lessons & videos</Link><h1 className="mt-6 text-3xl font-bold">Course sections</h1><p className="mt-2 text-slate-400">Create sections, then connect lessons to them.</p><SectionsManager courses={courses??[]} sections={sections??[]}/></main>}
