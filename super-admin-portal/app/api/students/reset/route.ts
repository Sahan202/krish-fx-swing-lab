import { NextRequest, NextResponse } from 'next/server';
import { audit, requireAdmin, serviceClient } from '@/lib/admin-audit';
export async function POST(request: NextRequest) { const actor = await requireAdmin(request); const { id } = await request.json() as { id?: string }; if (!actor) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 }); if (!id) return NextResponse.json({ error: 'Student ID is missing.' }, { status: 400 }); const password = `Kfx!${crypto.randomUUID().replaceAll('-', '').slice(0, 12)}a1`; const result = await serviceClient().auth.admin.updateUserById(id, { password }); if (result.error) return NextResponse.json({ error: result.error.message }, { status: 400 }); await audit(actor, 'STUDENT_PASSWORD_RESET', 'student', id); return NextResponse.json({ password }); }
